import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // MDX support is handled via next-mdx-remote
  // No additional config needed for MDX in App Router
};

export default nextConfig;
